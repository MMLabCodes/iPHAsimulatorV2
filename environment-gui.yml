name: pha_guichannels:  - conda-forgedependencies:  - python=3.11  - pip  - setuptools  - wheel  # GUI and data handling  - streamlit  - pandas  - numpy  - pyyaml  # Molecular display and GUI-side calculations  - rdkit  # Required currently because gui.config creates PHAFileManager,  # whose module imports ParmEd.  - parmed  - pip:      - -e .